# DEPRECATED — merged into monthly-data-sync.yml. Delete this file with
#   git rm .github/workflows/icann-reports.yml
# or run the [oneshot] Cleanup v3 workflow.
name: '[oneshot] DEPRECATED ICANN Reports (deleteme)'
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "This workflow has been merged into monthly-data-sync.yml. Delete this file."'
