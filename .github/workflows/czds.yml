# REMOVED — CZDS workflow dropped 2026-05-19. Delete with `git rm .github/workflows/czds.yml`.
# Kept as a stub with no triggers so GHA does not try to schedule it.
name: CZDS Ingest (DEPRECATED)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "This workflow has been deprecated; delete the file."'
